# md-en-kr Skill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 한글 rule/MD 파일을 압축된 영어 마크다운으로 변환하는 스킬을 단일 디렉토리 + plugin 형태로 구현해 Claude Code와 Codex CLI 양 플랫폼에서 동작하게 만든다.

**Architecture:** SKILL.md 한 파일에 frontmatter + 압축 규칙 + 호출 절차를 모두 인라인으로 담는다. Codex CLI는 AGENTS.md가 SKILL.md를 참조하도록 만들어 동일 절차를 공유한다. 외부 스크립트/API는 사용하지 않으며 변환은 호출한 에이전트가 직접 수행한다.

**Tech Stack:** Markdown, YAML frontmatter, git, bash (인라인 검증용)

**Spec:** `docs/superpowers/specs/2026-05-07-md-en-kr-design.md`

---

## File Structure

| 파일 | 역할 |
|---|---|
| `SKILL.md` | 진실의 원천. frontmatter + 압축 규칙 + 호출 절차. Claude Code 자동 인식 진입점. |
| `AGENTS.md` | Codex CLI 진입점. SKILL.md 절차를 따르라는 짧은 지시. |
| `plugin.json` | Claude Code plugin 메타데이터. marketplace 배포 대비. |
| `README.md` | 양 플랫폼 설치/사용 가이드. |
| `examples/before-ko.md` | 한글 입력 샘플 (검증용). |
| `examples/after-en.md` | 변환 후 영어 샘플 (검증용). |

---

## Task 1: examples 샘플 파일 작성

압축 규칙의 동작 기준점을 먼저 만들어, 이후 SKILL.md 작성 시 참조한다.

**Files:**
- Create: `examples/before-ko.md`
- Create: `examples/after-en.md`

- [ ] **Step 1: `examples/before-ko.md` 작성**

다음 내용을 그대로 작성한다:

```markdown
---
name: sample-rule
description: 한글 코딩 규칙 예시 파일입니다. 변환 검증용으로 사용합니다.
---

# 코딩 규칙

## 응답 언어
- 항상 한국어로 응답해야 합니다.
- 절대로 영어로 답변하지 마세요.

## 명명 규칙
- PHP/JS 변수와 함수는 camelCase를 사용합니다.
- 들여쓰기는 4칸 공백을 사용합니다.
- 약어 사용을 피하고 의도가 드러나는 이름을 사용합니다.

## 안전 동작
- `git push --force` 같은 파괴적 명령은 절대 사용하지 마세요.
- `/graphify` 명령 호출 시 `~/.claude/skills/graphify/SKILL.md`를 먼저 실행하세요.

## 트리거 문구
- 사용자가 `일반 말투`라고 하면 평소 톤으로 돌아갑니다.
```

- [ ] **Step 2: `examples/after-en.md` 작성**

다음 내용을 그대로 작성한다 (압축 규칙 적용 결과의 모범 답안):

````markdown
---
name: sample-rule
description: Sample Korean coding rules file used as a conversion verification fixture.
---

# Coding Rules

## Response Language
- Always respond in Korean.
- NEVER respond in English.

## Naming
- PHP/JS variables and functions: camelCase.
- Indentation: 4 spaces.
- Avoid abbreviations; use intention-revealing names.

## Safety
- NEVER use destructive commands like `git push --force`.
- On `/graphify`, run `~/.claude/skills/graphify/SKILL.md` first.

## Triggers
- When user says `일반 말투`, return to normal tone.
````

- [ ] **Step 3: 보존 항목 검증**

다음 명령으로 모든 보존 대상이 after-en.md에 존재하는지 확인:

```bash
for token in "camelCase" "/graphify" "~/.claude/skills/graphify/SKILL.md" "git push --force" "일반 말투" "name: sample-rule"; do
  grep -F "$token" examples/after-en.md > /dev/null && echo "OK: $token" || echo "MISSING: $token"
done
```

Expected: 전부 `OK:` 출력

- [ ] **Step 4: 커밋**

```bash
git add examples/before-ko.md examples/after-en.md
git commit -m "examples: add ko/en sample fixtures for compression verification"
```

---

## Task 2: SKILL.md 작성

진실의 원천. frontmatter, 압축 규칙, 호출 절차를 모두 인라인으로 포함.

**Files:**
- Create: `SKILL.md`

- [ ] **Step 1: `SKILL.md` 전체 작성**

````markdown
---
name: md-en-kr
description: Use when compressing Korean rule/MD files into compact English to reduce token usage. Triggers on "/compress-rule", "한글 룰 압축", "rule 영어 변환" requests, or when working with Korean .md rule files in skills/rules directories.
---

# md-en-kr

Compress Korean Markdown rule files into compact English while preserving every behaviorally meaningful instruction. Overwrites originals after user approval.

## Activation

- Slash command: `/compress-rule <path>`
- Natural language: "이 한글 rule 파일들 영어로 압축해줘", "compress these korean rules"
- Targets: single `.md` file, glob (`rules/*.md`), or directory (recursive)

## Procedure

### 1. Argument parsing
- Resolve the user-provided argument into a concrete file list.
- Single file: use as-is.
- Glob: expand via shell.
- Directory: collect all `*.md` recursively, excluding hidden directories (`.git`, `.venv`, `node_modules`, etc.).
- If the resolved list is empty, abort with a clear message.

### 2. Pre-flight check (MUST pass before any conversion)

Run these checks inline. Abort the entire run if any fail.

```bash
git rev-parse --is-inside-work-tree
```

For each target file:

```bash
git ls-files --error-unmatch <file>            # tracked?
git status --porcelain <file>                  # clean? (must produce empty output)
```

If any file is untracked or has uncommitted changes, list those files and abort. Reason: overwriting requires `git checkout` as the rollback path.

### 3. Per-file processing (sequential)

For each file in the resolved list:

1. Read the original file.
2. Apply the compression rules in §Rules to produce the English version.
3. Run the self-verification in §Self-Verification on the produced output. If any check fails, regenerate.
4. Show a unified diff (original vs. compressed) to the user.
5. Ask the user: `apply / skip / abort`.
   - `apply`: overwrite the original file with the compressed content.
   - `skip`: leave the file unchanged, continue to the next file.
   - `abort`: stop the run. Already-applied files remain (recoverable via git).

### 4. Summary

Report: applied N, skipped M, aborted? Include a length-ratio estimate per file (e.g., `42% of original`).

## Rules

### Core (preserve exactly)

- File paths
- Commands
- Code identifiers (class, method, function, variable, namespace names)
- Skill names
- Trigger phrases
- Output format examples
- Required response language rules
- Safety / destructive-action warnings
- Architecture constraints
- Coding conventions
- Korean phrases that are themselves part of the rule (e.g., `일반 말투`, `stop caveman`, `/graphify`)

### Compression behavior

- Compress aggressively; remove duplicated or redundant wording.
- Keep only behaviorally meaningful instructions.
- Prefer short imperative bullets over paragraphs.
- Use compact agent-friendly terms.
- Output Markdown only — no meta comments, no "Here is...", no commentary about the conversion itself.

### Markdown handling

- Preserve heading levels exactly. Do not renumber or restructure.
- Preserve link/image targets: `[text](path)` — translate `text`, leave `path` untouched.
- NEVER translate content inside inline code (`` ` ``) or fenced code blocks (```` ``` ````). They contain identifiers/commands.
- Code blocks count and content must match the original 1:1.

### YAML frontmatter

- Preserve the structure (keys and order).
- Translate Korean values into English (notably `description`).
- NEVER change the `name` value or any other identifier-like value.
- If a value is already English or non-text (boolean, number, list of identifiers), leave it.

### Safety wording

- Maintain the original strength. Map "절대", "반드시", "결코" to `MUST`, `NEVER`, `under no circumstances` as appropriate.
- Do not soften warnings.

### Deterministic vocabulary

Use consistent English terms across the output:

| 한글 | English |
|---|---|
| 스킬 | skill |
| 규칙 | rule |
| 트리거 | trigger |
| 호출 | invoke |
| 응답 | response |
| 검증 | verify / verification |
| 사용자 | user |
| 에이전트 | agent |

Extend this mapping consistently within a single file.

## Self-Verification

After producing the compressed output, the agent MUST verify before showing the diff:

1. Every file path token from the original (any string containing `/`) appears in the output.
2. Every fenced code block from the original is present with identical content.
3. Every inline-code token (`` `...` ``) from the original appears in the output.
4. Frontmatter `name` value is unchanged.
5. The required-Korean phrases listed in §Rules → Core are still present verbatim.

If any check fails, regenerate. Do not surface a failing diff to the user.

## Output writing

- Overwrite the original file with the compressed content on `apply`.
- Do not write any other file (no `.bak`, no sibling). Rollback path is `git checkout <file>`.

## Out of Scope

- Reverse direction (English → Korean).
- Non-`.md` files.
- Auto-applying without per-file approval.
- External LLM/API calls — the calling agent (Claude Code or Codex CLI) performs the conversion itself.
````

- [ ] **Step 2: frontmatter 유효성 확인**

```bash
head -5 SKILL.md
```

Expected: `---`로 시작, `name: md-en-kr`, `description:` 한 줄 (200자 미만), `---`로 닫힘.

- [ ] **Step 3: 보존 항목 셀프 검증**

SKILL.md 안에 양보 불가 토큰이 모두 있는지 확인:

```bash
for token in "/compress-rule" "git status --porcelain" "git ls-files --error-unmatch" "일반 말투" "stop caveman" "/graphify" "name:" "description:" "apply / skip / abort"; do
  grep -F "$token" SKILL.md > /dev/null && echo "OK: $token" || echo "MISSING: $token"
done
```

Expected: 전부 `OK:`

- [ ] **Step 4: 커밋**

```bash
git add SKILL.md
git commit -m "feat: add SKILL.md with compression rules and procedure"
```

---

## Task 3: AGENTS.md 작성 (Codex CLI 진입점)

Codex CLI가 AGENTS.md를 자동 컨텍스트로 포함하므로, SKILL.md 절차를 따르라는 짧은 지시만 둔다. 절차를 중복 작성하지 않는다 (DRY).

**Files:**
- Create: `AGENTS.md`

- [ ] **Step 1: `AGENTS.md` 작성**

```markdown
# md-en-kr (Codex CLI entry point)

This skill compresses Korean Markdown rule files into compact English to reduce token usage.

## How to use

When the user invokes `/compress-rule <path>` or asks to compress/translate Korean rule files, follow the procedure defined in `SKILL.md` exactly.

- Read `SKILL.md` in this directory before acting.
- Follow §Procedure step by step.
- Apply §Rules and §Self-Verification without modification.
- NEVER skip the pre-flight git-clean check.
- NEVER overwrite a file without showing the diff and getting `apply` from the user.

## Activation triggers

- `/compress-rule <path>`
- `/compress-rule <dir>`
- `/compress-rule "<glob>"`
- Natural language: "한글 룰 압축", "rule 영어 변환", "compress korean rules"
```

- [ ] **Step 2: 커밋**

```bash
git add AGENTS.md
git commit -m "feat: add AGENTS.md as Codex CLI entry point"
```

---

## Task 4: plugin.json 작성

Claude Code plugin marketplace 배포 대비. 지금은 메타데이터만 준비하고 등록은 별도 작업.

**Files:**
- Create: `plugin.json`

- [ ] **Step 1: `plugin.json` 작성**

```json
{
  "name": "md-en-kr",
  "version": "0.1.0",
  "description": "Compress Korean rule/MD files into compact English to reduce token usage. Works in Claude Code and Codex CLI.",
  "author": {
    "name": "moveju112",
    "email": "move@wemadeconnect.com"
  },
  "homepage": "https://github.com/moveju112/md-en-kr",
  "repository": {
    "type": "git",
    "url": "https://github.com/moveju112/md-en-kr.git"
  },
  "keywords": [
    "skill",
    "compression",
    "korean",
    "english",
    "markdown",
    "rules",
    "claude-code",
    "codex-cli"
  ],
  "license": "MIT"
}
```

- [ ] **Step 2: JSON 유효성 검사**

```bash
python3 -c "import json; json.load(open('plugin.json'))" && echo "valid JSON"
```

Expected: `valid JSON`

- [ ] **Step 3: 커밋**

```bash
git add plugin.json
git commit -m "feat: add plugin.json for Claude Code marketplace"
```

---

## Task 5: README.md 재작성

기존 README.md(Initial commit의 placeholder)를 양 플랫폼 설치/사용 가이드로 교체.

**Files:**
- Modify: `README.md`

- [ ] **Step 1: `README.md` 전체 교체**

```markdown
# md-en-kr

한글로 작성된 rule / MD 파일을 압축된 영어 마크다운으로 변환해 토큰 사용량을 줄이는 스킬. **Claude Code**와 **Codex CLI** 양쪽에서 동작합니다.

원본 파일을 직접 덮어쓰며, 변환 전 git clean 상태를 확인하고 파일별로 diff를 보여준 뒤 사용자 승인을 받습니다.

## 설치

### Claude Code

```bash
git clone https://github.com/moveju112/md-en-kr.git ~/.claude/skills/md-en-kr
```

설치 후 자동 인식되어 `description` 기반 자동 호출 + 직접 호출 모두 가능합니다.

### Codex CLI

```bash
git clone https://github.com/moveju112/md-en-kr.git ~/.codex/skills/md-en-kr
```

`AGENTS.md`가 진입점이며 Codex가 자동으로 컨텍스트에 포함합니다.

## 사용법

```
/compress-rule path/to/rule.md
/compress-rule path/to/dir/
/compress-rule "rules/*.md"
```

자연어로도 호출할 수 있습니다:

> 이 한글 rule 파일들 영어로 압축해줘: rules/*.md

## 동작

1. 인자(파일 / 글롭 / 디렉토리)를 파일 목록으로 확장합니다.
2. **각 파일이 git clean 상태인지 확인**합니다. 미커밋 변경이 있으면 전체 중단.
3. 파일별로:
   - 압축 규칙을 적용해 영어 버전 생성
   - 변환 결과 자가 검증 (경로 / 코드블록 / 인라인 코드 / frontmatter 보존 확인)
   - **diff 미리보기**를 보여주고 `apply / skip / abort` 선택
4. 처리 결과 요약 (적용 N개, 건너뜀 M개, 길이 비율).

## 보존 항목 (절대 변경하지 않음)

- 파일 경로, 명령어, 코드 식별자
- 스킬 이름, 트리거 문구, 출력 포맷 예시
- 안전 / 파괴적 동작 경고
- 코딩 컨벤션 / 아키텍처 제약
- 한글이 규칙 자체로 요구되는 경우 (예: `일반 말투`, `/graphify`)
- YAML frontmatter의 `name` 값과 키 구조

## 예시

- 입력: [`examples/before-ko.md`](examples/before-ko.md)
- 출력: [`examples/after-en.md`](examples/after-en.md)

## 안전

- 원본을 덮어쓰므로 **git 트래킹 + 클린 상태**가 필수입니다.
- 롤백은 `git checkout <file>` 한 줄로 가능합니다.
- 변환 도중 abort해도 이미 적용된 파일은 그대로 남습니다 (git으로 복구).

## 비범위

- 영어 → 한글 역변환
- `.md` 외 파일 (`.txt`, `.rst` 등)
- 사용자 승인 없는 일괄 자동 적용
- 외부 LLM/API 호출 (호출한 에이전트가 직접 변환)
```

- [ ] **Step 2: 링크 유효성 확인**

```bash
for f in examples/before-ko.md examples/after-en.md; do
  test -f "$f" && echo "OK: $f" || echo "MISSING: $f"
done
```

Expected: 둘 다 `OK:`

- [ ] **Step 3: 커밋**

```bash
git add README.md
git commit -m "docs: rewrite README with install and usage for both platforms"
```

---

## Task 6: 통합 검증 (드라이런)

실제 변환 동작을 직접 시연하지는 않고, 모든 파일이 일관되게 맞물리는지 정적 검증한다.

**Files:**
- Test: 리포 전체

- [ ] **Step 1: 모든 핵심 파일 존재 확인**

```bash
for f in SKILL.md AGENTS.md plugin.json README.md examples/before-ko.md examples/after-en.md docs/superpowers/specs/2026-05-07-md-en-kr-design.md docs/superpowers/plans/2026-05-07-md-en-kr.md; do
  test -f "$f" && echo "OK: $f" || echo "MISSING: $f"
done
```

Expected: 전부 `OK:`

- [ ] **Step 2: SKILL.md / AGENTS.md / README.md 사이 트리거 일관성 확인**

세 파일 모두 `/compress-rule`을 동일하게 사용하는지 확인:

```bash
grep -l "/compress-rule" SKILL.md AGENTS.md README.md
```

Expected: 세 파일 모두 출력됨.

- [ ] **Step 3: examples 검증 — `name` 보존**

`before-ko.md`와 `after-en.md`의 `name` 값이 동일한지 확인:

```bash
grep "^name:" examples/before-ko.md examples/after-en.md
```

Expected: 두 줄 모두 `name: sample-rule`.

- [ ] **Step 4: examples 검증 — 한글 트리거 보존**

```bash
grep -F "일반 말투" examples/after-en.md
```

Expected: `일반 말투`가 포함된 줄이 출력됨.

- [ ] **Step 5: examples 검증 — 코드블록/명령어 보존**

```bash
for token in "git push --force" "/graphify" "camelCase" "~/.claude/skills/graphify/SKILL.md"; do
  grep -F "$token" examples/after-en.md > /dev/null && echo "OK: $token" || echo "MISSING: $token"
done
```

Expected: 전부 `OK:`

- [ ] **Step 6: examples 검증 — 길이 비율 확인 (압축 효과)**

```bash
ko=$(wc -c < examples/before-ko.md)
en=$(wc -c < examples/after-en.md)
echo "ko=$ko en=$en ratio=$(awk "BEGIN{printf \"%.0f%%\", $en/$ko*100}")"
```

Expected: `en` < `ko` (즉 ratio < 100%). 100% 이상이면 examples를 다시 다듬는다.

- [ ] **Step 7: plugin.json 유효성 재확인**

```bash
python3 -c "import json; d=json.load(open('plugin.json')); assert d['name']=='md-en-kr'; print('valid')"
```

Expected: `valid`

- [ ] **Step 8: git 상태 클린 확인 후 push**

```bash
git status
git push origin main
```

Expected: working tree clean, push 성공.

- [ ] **Step 9: 완료 보고**

다음을 사용자에게 보고:
- 추가된 파일 목록
- 커밋 수
- 원격 push URL
- 다음 단계 안내 (Claude Code / Codex CLI 설치 명령)

---

## Self-Review (작성자 자체 점검)

- ✅ 스펙 §3 리포 구조의 모든 파일이 Task로 매핑됨 (Task 1: examples, 2: SKILL.md, 3: AGENTS.md, 4: plugin.json, 5: README.md).
- ✅ 스펙 §4.2 절차(인자 파싱 → 사전 검증 → 파일별 처리 → 요약)가 SKILL.md §Procedure에 그대로 들어감.
- ✅ 스펙 §5.2 보강 규칙 7가지가 SKILL.md §Rules에 항목별로 반영됨.
- ✅ 스펙 §5.3 자가 검증 4항목이 SKILL.md §Self-Verification에 반영됨.
- ✅ 스펙 §6 양 플랫폼 설치가 README.md와 AGENTS.md로 분리 반영됨.
- ✅ 스펙 §8 비범위가 SKILL.md §Out of Scope와 README.md §비범위에 반영됨.
- ✅ 모든 Task가 구체적인 파일 경로와 커밋 명령을 포함, placeholder 없음.
- ✅ 타입/식별자 일관성: `/compress-rule`, `apply / skip / abort`, `name: md-en-kr` 등 전체에서 동일 사용.
